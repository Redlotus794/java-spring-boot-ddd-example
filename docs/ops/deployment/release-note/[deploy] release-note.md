# 版本发布记录 - Release Note

对于版本发布频率较高的项目，采用每个版本一个文件的方式记录版本发布过程。
对于频率较低的项目，可以采用一个文件记录所有版本发布过程。

## 新增功能
- 支持批量执行Yaak桌面端定义的API请求集合
- 新增JSON格式结果导出，可直接用于脚本处理

## 优化
- 减少命令行执行时的内存占用（降低25%）

## Bug修复
- 修复指定环境变量时参数失效的问题

## 注意事项
- 需搭配Yaak桌面端V1.5.0及以上版本使用